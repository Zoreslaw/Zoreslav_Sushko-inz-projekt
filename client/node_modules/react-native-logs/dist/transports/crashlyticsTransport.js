"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.crashlyticsTransport = void 0;
const crashlyticsTransport = (props) => {
    var _a, _b, _c;
    if (!props)
        return false;
    if (!((_a = props === null || props === void 0 ? void 0 : props.options) === null || _a === void 0 ? void 0 : _a.CRASHLYTICS)) {
        throw Error(`react-native-logs: crashlyticsTransport - No crashlytics instance provided`);
    }
    let isError = true;
    if ((_b = props === null || props === void 0 ? void 0 : props.options) === null || _b === void 0 ? void 0 : _b.errorLevels) {
        isError = false;
        if (Array.isArray((_c = props === null || props === void 0 ? void 0 : props.options) === null || _c === void 0 ? void 0 : _c.errorLevels)) {
            if (props.options.errorLevels.includes(props.level.text)) {
                isError = true;
            }
        }
        else {
            if (props.options.errorLevels === props.level.text) {
                isError = true;
            }
        }
    }
    try {
        if (isError) {
            props.options.CRASHLYTICS.recordError(props.msg);
        }
        else {
            props.options.CRASHLYTICS.log(props.msg);
        }
        return true;
    }
    catch (error) {
        throw Error(`react-native-logs: crashlyticsTransport - Error on send msg to crashlytics`);
    }
};
exports.crashlyticsTransport = crashlyticsTransport;
